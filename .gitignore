# OS junk
.DS_Store
**/.DS_Store
"untitled folder/"

# Real customer quotations — DO NOT publish
test-quotes/

# Dev-only scripts & scratch (not needed for the live site)
tools/
*.log

# local ship helper (may hold a Render deploy hook)
ship.sh

# ML pipeline working dir (durable, not for the site)
pipeline/
